<h2 class="text-3xl font-bold text-indigo-700 mb-6">✉️ 新規メッセージ</h2>

<%= render 'form', message: @message %>

<div class="mt-4">
  <%= link_to "← 一覧へ", messages_path, class: "text-gray-500 hover:text-gray-700 underline text-sm" %>
</div>
